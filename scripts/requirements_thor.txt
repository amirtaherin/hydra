# Hydra dependencies — Thor (JetPack 7.1, CUDA 12.8)
# Paper stack: JetPack 7.1, CUDA 13.2, torch 2.11.0 (jetson-ai-lab sbsa/cu130)
# PyTorch is NOT pip-installable from PyPI on Jetson - use the
# platform wheel noted above (see README).
# Install these with: pip3 install -r scripts/requirements_thor.txt

# HF profiler
transformers==5.5.4
tokenizers==0.22.2
accelerate>=0.26.0
huggingface_hub
numpy
pandas>=2.0
seaborn
matplotlib

# llama.cpp profiler (build dependencies, install via apt)
# sudo apt install cmake build-essential wget curl

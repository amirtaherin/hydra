# Hydra dependencies — AGX Xavier (JetPack 5, CUDA 11.4)
# Paper stack: JetPack 5.1, CUDA 11.4, torch 2.2.0 (NVIDIA JP5 wheel)
# PyTorch is NOT pip-installable from PyPI on Jetson - use the
# platform wheel noted above (see README).
# Install these with: pip3 install -r scripts/requirements_xavier.txt

# HF profiler
transformers==4.46.3
tokenizers==0.20.3
accelerate<1.0            # 1.0+ dropped Python 3.8
huggingface_hub<0.30      # 0.30+ pulls hf-xet (no py3.8/aarch64 wheel)
numpy<2
pandas>=2.0
seaborn
matplotlib

# llama.cpp profiler (build dependencies, install via apt)
# sudo apt install cmake build-essential wget curl

# Python 3.8 stdlib backport (zoneinfo landed in 3.9)
backports.zoneinfo; python_version < "3.9"

# Hydra dependencies — AGX Orin (JetPack 6, CUDA 12.6)
# Paper stack: JetPack 6.2, CUDA 12.6, torch 2.3.0 (NVIDIA JP6 wheel)
# PyTorch is NOT pip-installable from PyPI on Jetson - use the
# platform wheel noted above (see README).
# Install these with: pip3 install -r scripts/requirements_orin.txt

# HF profiler
transformers==4.51.3
tokenizers==0.21.1
accelerate>=0.26.0
huggingface_hub
numpy<2
pandas>=2.0
seaborn
matplotlib

# llama.cpp profiler (build dependencies, install via apt)
# sudo apt install cmake build-essential wget curl
